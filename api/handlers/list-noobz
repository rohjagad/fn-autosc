#!/bin/bash
# FN-API: list NoobzVPN accounts (text body, as noobzvpns prints it).
. /usr/local/lib/fn-api/lib.sh
out="$(noobzvpns print-all 2>/dev/null || noobzvpns --info-all-user 2>/dev/null)"
jq -nc --arg t "$out" '{status:"success",text:$t}'
